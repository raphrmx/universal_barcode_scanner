include: package:lint/strict.yaml

analyzer:
  errors:
    avoid_classes_with_only_static_members: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
